let lines = 2;
let line = 4;
let X = parseInt(line[0]);
let Y = parseInt(line[1]);
 
var price = 0;

    if (X ==   1  ) {
      price  =  4,00       ;
    }
    else if (X ==  2  ) {
      price  =   4.5        ;
    }
    else if (X == 3    ) {
      price  =  5.00           ;
    }
    else if (X == 4   ) {
      price  =  2.00        ;
    }
    else if (X ==   5  ) {
      price  =      1.5        ;
    }
    

    Y *= X
    
    var price = Y
    
    
    console.log( "     Total:            "  + "R$" + price      .toFixed(   ));


//Escreva o seu código no espaço em branco